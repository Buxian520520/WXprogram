"""
生成小程序介绍文案 Word 文档 — 可直接对着念的演讲稿
"""
from docx import Document
from docx.shared import Pt, Cm, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn

doc = Document()

# ========== 样式工具 ==========
def set_font(run, cn_font, en_font, size_pt, bold=False, color=None):
    run.font.size = Pt(size_pt)
    run.bold = bold
    run.font.name = en_font
    rPr = run._element.get_or_add_rPr()
    from docx.oxml import OxmlElement
    rFonts = rPr.find(qn('w:rFonts'))
    if rFonts is None:
        rFonts = OxmlElement('w:rFonts')
        rPr.insert(0, rFonts)
    rFonts.set(qn('w:eastAsia'), cn_font)
    rFonts.set(qn('w:ascii'), en_font)
    rFonts.set(qn('w:hAnsi'), en_font)
    if color:
        run.font.color.rgb = color

def add_title(text, level=1):
    """添加标题"""
    h = doc.add_heading(text, level=level)
    h.alignment = WD_ALIGN_PARAGRAPH.LEFT
    for run in h.runs:
        if level == 0:
            set_font(run, '黑体', 'Arial', 22, bold=True)
        elif level == 1:
            set_font(run, '黑体', 'Arial', 16, bold=True)
        elif level == 2:
            set_font(run, '黑体', 'Arial', 14, bold=True)
    return h

def add_para(text, indent=True):
    """添加正文段落 — 宋体/Consolas 小四 首行缩进"""
    p = doc.add_paragraph()
    p.paragraph_format.first_line_indent = Cm(0.74) if indent else Cm(0)
    p.paragraph_format.line_spacing = Pt(22)
    p.paragraph_format.space_before = Pt(4)
    p.paragraph_format.space_after = Pt(4)
    run = p.add_run(text)
    set_font(run, '宋体', 'Consolas', 12)
    return p

def add_bullet(text, indent=True):
    """添加要点"""
    p = doc.add_paragraph()
    p.paragraph_format.first_line_indent = Cm(0.74) if indent else Cm(0)
    p.paragraph_format.left_indent = Cm(1.5)
    p.paragraph_format.line_spacing = Pt(22)
    p.paragraph_format.space_before = Pt(2)
    p.paragraph_format.space_after = Pt(2)
    run = p.add_run('● ' + text)
    set_font(run, '宋体', 'Consolas', 12)
    return p

# ================================================================
# 封面
# ================================================================
p = doc.add_paragraph()
p.alignment = WD_ALIGN_PARAGRAPH.CENTER
p.paragraph_format.space_before = Pt(120)
run = p.add_run('NETMAXXX 学生管理系统')
set_font(run, '黑体', 'Arial', 26, bold=True, color=RGBColor(0x16, 0x77, 0xFF))

p2 = doc.add_paragraph()
p2.alignment = WD_ALIGN_PARAGRAPH.CENTER
run2 = p2.add_run('微信小程序演示讲解稿')
set_font(run2, '宋体', 'Consolas', 16, color=RGBColor(0x66, 0x66, 0x66))

doc.add_paragraph()

# ================================================================
# 一、项目概述
# ================================================================
add_title('一、项目概述', level=1)

add_para('大家好，我今天展示的项目是 NETMAXXX 学生管理系统。这是一个基于微信小程序平台开发的、面向高校师生日常管理场景的综合信息管理系统。系统支持学生和教师两种角色，涵盖了从学生档案管理、课堂签到、请假审批到数据统计分析、AI 智能对话等完整的校园管理功能。')

add_para('系统采用前后端分离架构。前端是我们现在看到的微信小程序，使用微信原生框架开发，包含 WXML 模板语言、WXSS 样式系统和 JavaScript 逻辑层。后端基于 Django 5.2 框架构建，使用 MySQL 数据库，通过 RESTful API 与前端通信，并采用 JWT Token 实现无状态的用户认证。整个后端通过 natapp 内网穿透服务部署到公网，使得小程序可以随时随地进行数据交互。')

# ================================================================
# 二、角色与权限体系
# ================================================================
add_title('二、角色与权限体系', level=1)

add_para('系统设计了学生和教师两种角色，两种角色在登录后看到完全不同的功能界面。')

add_para('教师端的功能包括：学生档案的增删改查、Excel 批量导入导出、讲师个人信息管理、请假审批管理、课堂签到任务的发布与管理、以及数据可视化看板。教师在系统中扮演管理者的角色，拥有最高的数据操作权限。')

add_para('学生端的功能包括：查看个人档案信息、扫码或定位进行课堂签到、提交请假申请并查看审批进度、查看自己的签到历史记录等。学生的操作权限被严格限定在个人数据范围内，例如学生只能查看自己的信息，只能删除自己尚未审批的请假记录。')

add_para('这种基于角色的权限控制在后端通过 JWT Token 中的 role 字段实现。每次 API 请求时，后端会解析 Token 获取用户角色，在视图函数中进行角色校验。例如在学生列表接口中，学生角色只能返回自己的信息，而教师角色可以获取全部学生数据。这种设计既保证了数据安全，又避免了前端路由层面的越权风险。')

# ================================================================
# 三、学生信息管理
# ================================================================
add_title('三、学生信息管理', level=1)

add_para('学生信息管理是整个系统的基础功能模块。学生档案包含学号、姓名、性别、出生日期、手机号码、邮箱地址、家庭住址和头像照片八个字段。其中学号采用 95 开头的五位数编码规则，通过前端正则验证和后端唯一性校验双重保障学号的合法性。')

add_para('在学生列表页面，教师可以进行多维度搜索——输入学号、姓名、性别、手机号或邮箱中的任意关键词都能触发模糊查询。列表支持分页展示，可以按每页 5 条、10 条、50 条或 100 条进行切换。同时支持批量选择和批量删除操作，全选逻辑基于当前页实现，既方便操作又防止误操作。')

add_para('数据导入导出方面，系统通过 openpyxl 库实现了 Excel 的读写功能。导入时支持有表头和没有表头两种格式，能够自动识别中文或英文字段名并映射到对应的数据列。导入过程包含完整的容错机制：遇到异常行不会中断整个导入流程，而是记录错误并继续处理后续数据，最后向教师汇报成功数与失败数的统计结果。导出功能则通过微信的文件分享接口，支持直接转发给微信好友或使用系统查看器打开。')

add_para('在数据库层面，学生注册时系统会自动完成三项操作：在学生表中创建档案记录、在用户表中创建登录账号、以及使用 bcrypt 对密码进行哈希加密。默认密码设为 123456，学生可以在后续自行修改。')

# ================================================================
# 四、签到功能（重点）
# ================================================================
add_title('四、签到功能', level=1)

add_para('签到功能是本系统的一大核心亮点，接下来我重点介绍这个模块的设计与实现。签到模块支持两种签到方式：二维码签到和位置签到，覆盖了课堂教学中最常见的两种考勤场景。')

add_title('4.1 教师发布签到', level=2)

add_para('教师进入签到页面后，可以创建新的签到任务。创建时需要填写任务标题，选择签到方式。如果选择二维码签到，系统会生成一个唯一的签到码，学生通过输入该签到码或扫描教师端展示的二维码图片完成签到。这个二维码是通过后端动态生成的，包含了任务ID和签到码信息。')

add_para('如果选择位置签到，教师需要授权 GPS 定位权限。系统获取教师当前的经纬度坐标，同时设置一个签到范围——默认 100 米，教师可以通过滑块在 10 米到 2000 米之间调整。这个范围的含义是：学生必须在以教师位置为圆心、以该范围为半径的圆形区域内才能成功签到。')

add_para('签到任务发布后进入"进行中"状态，教师可以实时查看已签到人数。当课堂结束时，教师点击"结束"按钮，任务状态变为"已结束"，此后学生将无法再进行签到。')

add_title('4.2 学生完成签到', level=2)

add_para('对于二维码签到，学生有两种操作方式：一是手动输入签到码，二是直接调用微信的扫码 API 扫描教师端的二维码图片。扫码功能使用 wx.scanCode 接口，设置仅允许从摄像头扫描，保证了签到码的物理传递性，避免学生通过复制粘贴绕过课堂场景。')

add_para('对于位置签到，学生点击"获取位置并签到"按钮后，系统调用微信的 GPS 定位接口获取学生的经纬度坐标。这里有一个精心设计的用户体验流程：获取位置后会打开一个全屏地图界面，地图上同时展示学生的位置标记和教师发布任务时的位置标记，签到有效范围以半透明圆环形式可视化呈现。学生可以直观地确认自己是否在签到范围内，然后点击"确认签到"按钮提交。')

add_para('定位权限的处理是本模块的一个技术难点。微信小程序的定位权限有三种状态：未请求、已授权、已拒绝。系统通过 wx.getSetting 接口先检查权限状态，如果用户之前拒绝过定位权限，会弹出引导对话框，指引用户跳转到系统设置页面手动开启。这种闭环的权限处理流程避免了因权限问题导致的签到失败。')

add_title('4.3 教师考勤管理', level=2)

add_para('签到任务结束后，教师可以从多个维度查看和管理考勤数据。首先是签到记录查看——可以看到每个已签到学生的姓名、学号和签到时间。对于位置签到任务，教师还可以打开地图视图，在地图上看到所有学生的签到位置标记，同时展示签到范围圈。地图标记带有数字编号，点击标记可以查看对应的学生信息。')

add_para('其次是考勤详情面板——这是一个更全面的视图，系统会将班级全部学生的名单与签到记录进行比对，清晰展示每个学生的签到状态：绿色标签表示已签到并显示签到时间，红色标签表示未签到。教师可以在此面板中执行补签和删除记录操作。补签功能用于处理学生因技术故障无法签到但确实在场的情况，由教师手动为学生补充签到记录。')

add_para('历史签到任务会保存在"历史签到记录"区域，教师可以随时回溯查看过往的考勤数据，也可以删除不再需要的签到任务。删除操作会同时清除该任务下的所有签到记录，系统会弹出二次确认对话框防止误删。')

# ================================================================
# 五、请假功能（重点）
# ================================================================
add_title('五、请假功能', level=1)

add_para('请假管理是另一个核心功能模块，实现了从学生提交申请到教师审批的完整业务流程。接下来我从学生端和教师端两个视角分别介绍。')

add_title('5.1 学生端：提交请假申请', level=2)

add_para('学生进入请假页面后，首先看到一个可折叠的申请表单。表单包含四个必填项：请假类型通过下拉选择器提供——包括事假、病假和其他三种类型；开始日期和结束日期使用日期选择器，并且系统会自动校验结束日期不能早于开始日期；请假原因是一个多行文本输入框，要求至少输入 5 个字符，最多 500 个字符。表单提交前会进行完整的前端校验，校验不通过时会在对应字段下方显示红色错误提示。')

add_para('申请提交后，后端会自动计算请假天数——通过结束日期减去开始日期再加一天得到。系统还将学生的姓名和学号冗余存储到请假表中，这样做虽然牺牲了一些数据库范式，但带来了查询性能的提升：教师在查看请假列表时不需要再关联查询学生表，尤其是在按学生姓名搜索时效率更高。')

add_para('学生可以在"我的请假记录"区域查看所有请假申请及其审批状态。系统提供了状态筛选功能，可以按"全部"、"待审批"、"已批准"、"已拒绝"四个维度进行过滤。每条记录显示请假类型、日期范围、天数、状态标签和申请时间。对于待审批状态的记录，学生可以主动删除；已审批的记录则不允许删除，只能查看详情。')

add_title('5.2 教师端：审批请假', level=2)

add_para('教师的请假管理页面设计了一个功能完善的审批工作台。页面顶部有搜索栏和状态下拉筛选器，教师可以按学生姓名搜索或按审批状态筛选。请假列表以卡片形式展示，每张卡片包含学生姓名、请假类型标签、状态标签、日期范围和请假原因摘要。卡片左侧有复选框，支持多选后进行批量删除。')

add_para('审批操作是教师端的核心功能。对于待审批状态的请假记录，卡片右侧会显示绿色的"批准"按钮和红色的"拒绝"按钮。点击任一按钮会弹出一个审批对话框，教师可以选择性地填写审批意见，然后确认提交。后端在处理审批时，会同时更新请假记录的状态、审批意见、审批时间和审批教师 ID 四个字段。如果请假已经被处理过，系统会返回提示"该请假记录已处理"，防止重复审批。')

add_para('详情对话框提供了请假记录的完整信息，包括学生姓名、学号、请假类型、日期、天数、原因以及审批意见和审批时间。在详情对话框中，教师也可以直接进行批准或拒绝操作，这个设计减少了操作步骤，提升了使用效率。')

# ================================================================
# 六、AI 对话功能（重点）
# ================================================================
add_title('六、AI 对话功能', level=1)

add_para('AI 智能对话是本系统的一个创新亮点功能，通过接入 DeepSeek 大语言模型，为师生提供智能问答服务。这个功能以悬浮面板的形式集成在系统的多个核心页面中，用户可以在不离开当前页面的情况下随时唤起 AI 助手进行对话。')

add_title('6.1 技术实现', level=2)

add_para('AI 对话的技术实现分为前后端两个部分。在前端，我们封装了一个独立的 AI 工具模块——utils/ai.js。该模块内置了 DeepSeek API 的调用逻辑，默认使用 deepseek-chat 模型，通过 wx.request 发起 HTTP POST 请求。模块暴露了两个核心方法：chat 方法支持多轮对话，接收完整的消息列表，每条消息包含 role 和 content 字段，role 可以是 system、user 或 assistant；ask 方法则是对 chat 的简化封装，用于快速单轮问答。')

add_para('在对话参数方面，我们设置了 temperature 为 0.7，这是一个在创造性和准确性之间取得平衡的值；max_tokens 设置为 2000，确保回复内容足够详实但又不会过长。API Key 采用内置默认值加可选自定义的设计，用户如果需要可以使用自己的 Key。')

add_title('6.2 界面交互', level=2)

add_para('AI 对话的交互界面采用了悬浮面板的设计模式。这个面板是一个自定义组件 floating-panel，封装了完整的对话 UI 逻辑。面板在默认状态下以一个小圆形触发按钮的形式悬浮在页面右下角，点击后展开为占据屏幕下半部分的对话窗口。窗口顶部有标题栏，中间是对话消息的滚动列表，底部是输入区域。')

add_para('对话界面的消息气泡采用了微信聊天风格的左右布局：用户发送的消息显示在右侧，使用蓝色背景；AI 的回复显示在左侧，使用白色背景。消息内容支持多行文本的自然换行，整个对话区域可以上下滚动查看历史消息。')

add_para('用户在个人中心页面点击"AI 对话"菜单项后，系统会通过全局状态标记启用 AI 功能。这个标记存储在 app.globalData.aiDialogEnabled 中，所有页面实例共享同一份数据。当用户切换到其他页面时，页面生命周期的 onShow 方法会检测该标记并自动显示悬浮面板，实现了跨页面的 AI 对话连续性。')

add_title('6.3 应用场景', level=2)

add_para('AI 对话可以应用于多种校园场景。比如学生可以向 AI 咨询课程安排、查询考试成绩趋势、了解请假流程等；教师可以利用 AI 辅助分析班级数据、生成学生评价建议、获取教学资源推荐。这个功能的设计思路是将大语言模型的通用能力与校园管理系统的具体业务场景相结合，为用户提供一个随时可用的智能助手。')

# ================================================================
# 七、数据图表展示（重点）
# ================================================================
add_title('七、数据图表展示', level=1)

add_para('数据可视化看板是教师专属的功能模块，通过图表直观呈现学生数据的统计分布。页面加载时会并行发起四个 API 请求，分别获取性别分布、年龄分布、生日月份分布和近 15 天请假趋势的数据。')

add_title('7.1 图表类型与数据来源', level=2)

add_para('第一张图表是班级男女生比例饼图。数据来自后端对 Student 表中 Gender 字段的 COUNT 聚合统计，结果以键值对数组的形式返回——格式为 [{name: "男", value: 人数}, {name: "女", value: 人数}]。饼图采用环形设计，内外半径比例约 45% 到 70%，每个扇区标注类别名称和百分比。')

add_para('第二张图表是学生年龄分布饼图。后端通过遍历所有学生的生日字段，逐一计算当前年龄——用当前年份减去出生年份，再根据月日比较进行修正——然后按年龄值分组统计。例如 18 岁的学生有 20 人、19 岁的有 35 人，以此类推。图表使用多种颜色区分不同年龄段，让年龄结构一目了然。')

add_para('第三张图表是班级生日月份分析柱状图。后端使用 Django ORM 的 ExtractMonth 函数从生日字段中提取月份信息，然后按月聚合计数，最后映射为一月到十二月的中文名称。柱状图使用蓝色填充，X 轴标签旋转 30 度以避免月份名称重叠。这张图可以直观反映班级学生的出生月份分布，对于组织生日活动等场景很有参考价值。')

add_para('第四张图表是近 15 天请假人数趋势折线图。后端查询 Leave 表中 apply_time 在最近 15 天内的记录，按天截断日期并统计每天的请假人数。对于没有请假记录的日期，自动补零以保证时间轴的连续性。折线图使用绿色线条，下方填充半透明绿色区域，数据点用实心圆标记。')

add_title('7.2 双引擎渲染方案', level=2)

add_para('图表渲染方面，我们设计了一个双引擎方案来兼顾效果和兼容性。首选引擎是 ECharts——这是一个功能强大的数据可视化库，我们通过社区提供的 ec-canvas 组件将其适配到微信小程序中。ec-canvas 组件内部使用 Canvas 2D API 创建画布，并在 onInit 回调中将画布实例传递给 ECharts 进行初始化。每个图表的配置包括 tooltip 提示框、legend 图例、series 数据系列等完整的 ECharts 选项。')

add_para('但考虑到 ECharts 库体积较大——压缩后也有约 500KB，可能会增加小程序的加载时间，某些情况下用户也可能未安装该库。为此我们实现了一套完整的 Canvas 2D 降级方案。降级方案使用微信小程序原生的 Canvas 2D 接口手绘图表：饼图通过 arc 方法绘制扇形并填充不同颜色；柱状图通过 fillRect 方法绘制矩形，同时绘制网格线和数值标注；折线图通过 moveTo 和 lineTo 方法连接数据点，并填充下方区域形成面积图效果。所有 Canvas 绘制都适配了设备的像素比，保证在高低分辨率屏幕上都有清晰的显示效果。')

add_para('图表数据加载采用了 Promise.all 并行请求的策略。四个图表的数据接口同时发起，只有当所有请求都完成后才进行图表初始化，避免了数据到达时间不一致导致的渲染不同步问题。加载过程中显示"正在加载图表数据"的提示，加载完成后一次性渲染全部四个图表。')

# ================================================================
# 八、技术实现细节
# ================================================================
add_title('八、技术实现细节', level=1)

add_title('8.1 Token 管理与自动刷新', level=2)

add_para('系统采用 JWT 双 Token 机制进行身份认证。Access Token 有效期 30 分钟，Refresh Token 有效期 7 天。前端 API 模块封装了一套自动刷新机制：当请求返回 401 状态码时，模块自动使用 Refresh Token 请求新的 Access Token，成功后重放原始请求。如果同时有多个请求因 Token 过期而失败，模块会将这些请求加入等待队列，等刷新完成后统一重试，避免了多个并发请求同时触发多次刷新的问题。')

add_title('8.2 自定义 TabBar', level=2)

add_para('系统的底部导航栏采用了微信小程序的自定义 TabBar 机制。与原生 TabBar 不同，自定义 TabBar 可以根据用户角色动态调整显示的标签页。教师端显示学生信息、数据中心、签到和我的四个标签，学生端显示签到、请假和我的三个标签。TabBar 组件通过监听 app.globalData 中的 userRole 变化来动态更新标签列表，同时根据当前页面路径自动高亮对应的标签。')

add_title('8.3 数据安全设计', level=2)

add_para('在数据安全方面，系统实施了多层防护措施。首先，密码使用 bcrypt 算法进行哈希加密存储，这是一种专门设计用于密码存储的慢哈希算法，能够有效抵御暴力破解。其次，后端每个接口都进行了角色权限校验——不仅在获取数据时根据角色过滤结果，在修改和删除操作前也会验证操作者的身份和权限。前端的角色路由只是一层 UI 限制，真正的安全防线在后端。')

add_para('另外，学生和教师账号与各自的业务表通过 user_id 字段关联。注册时系统自动完成关联，登录时如果检测到 student 角色的账号未绑定学号，会拒绝登录并给出明确的错误提示。这保证了用户体系的完整性和数据一致性。')

# ================================================================
# 九、总结
# ================================================================
add_title('九、总结', level=1)

add_para('NETMAXXX 学生管理系统是一个功能完整、架构清晰的微信小程序项目。它综合运用了微信小程序原生开发、Django RESTful API、MySQL 数据库、JWT 认证、ECharts 数据可视化、Canvas 绘图、DeepSeek AI 对话、GPS 定位与地图、二维码生成与扫描等多种技术。')

add_para('系统最核心的四个功能模块——签到、请假、AI 对话和数据图表——各有其技术特色：签到模块深入集成了微信的定位和扫码硬件能力，请假模块实现了完整的审批工作流，AI 对话通过悬浮面板提供了随时可用的智能助手，数据图表则通过双引擎渲染方案兼顾了效果与兼容性。')

add_para('整个项目的代码结构清晰，前后端职责分明，权限体系完善，用户交互体验细致——从表单校验的实时反馈、定位权限的闭环处理、数据删除的二次确认、到加载状态的视觉提示，每个细节都经过了精心设计。感谢大家的聆听，如果有什么问题，欢迎提问。')

# ========== 保存 ==========
output_path = r'N:\课程备份\小程序\大作业\小程序演示讲解稿.docx'
doc.save(output_path)
print(f'[OK] Saved to: {output_path}')
